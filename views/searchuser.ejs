<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <h1 class="mb-20 font-bold">Your Search User Is Here:</h1>
    <% if (user) { %>
        <div class="mb-4 ml-32 mt-2 flex items-center">
            <div class="w-24 h-24 rounded-full bg-gray-700 flex items-center justify-center overflow-hidden">
                <img class="rounded-full" src="<%= user.image %>" alt="<%= user.name %>">
            </div>
            <div class="ml-4">
                <div class="font-bold">
                    <a class="text-blue-500" href="/profile/<%= user._id %>"><%= user.name %></a>
                </div>
                <button class="text-gray-500"><%= user.username %></button>
            </div>
        </div>
    <% } else { %>
        <p>No user found</p>
    <% } %>
</div>
</body>
</html>
