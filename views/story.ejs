<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Story</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white">
    <div class="p-4">
        <a class="text-blue-500 mt-10 ml-5 block" href="/allpost">back to home</a>
        <!-- Posts -->
        <div class="flex-1 overflow-y-auto p-4 flex flex-col items-center">
            <div class="bg-gray-700 p-4 rounded-lg mb-4 w-full max-w-xl">
                <div class="flex mb-4 items-center">
                    <div class="w-10 h-8 rounded-full bg-gray-700 flex items-center justify-center overflow-hidden">
                        <img class="rounded-full" src="<%= story.user.image %>" alt="<%= story.user.name %>">
                    </div>
                    <div class="ml-4">
                        <a class="text-white" href="#"><%= story.user.username %></a>
                        <div class="text-gray-500 text-sm"><%= story.createdAt.toLocaleString('en-US', { hour: '2-digit', minute: '2-digit', hour12: true }) %></div>

                    </div>
                </div>
                <div class="mb-2 flex justify-center">
                    <img src="<%= story.image %>" alt="Post Image" class="rounded-lg mb-4 object-cover" style="width: 470px; height: 470px;">
                </div>
            </div>
        </div>
    </div>
</body>
</html>
